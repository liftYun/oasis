server:
  port: ${SERVER_PORT}
swagger:
  uri: "/"

spring:
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

  # ?? ??? Redis
  #  data:
  #    redis:
  #      host: ${REDIS_HOST}
  #      port: ${REDIS_PORT}
  #      password: ${REDIS_PASSWORD}
  #      timeout: 20000  # ?? ???? (ms)
  datasource:
    url: ${DATASOURCE_URL}
    username: ${DATASOURCE_USERNAME}
    password: ${DATASOURCE_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
  jwt:
    secret: ${JWT_SECRET_KEY}

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}      # ???? ?? ?? ?
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope:
              - openid
              - profile
              - email
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"
            client-name: Google

jwt:
  access:
    expired-ms: 600000    # 10?
  refresh:
    expired-ms: 604800000 # 7?
logging:
  level:
    org.hibernate.SQL: debug #?? SQL ??? ??? ??
    org.hibernate.type: trace #SQL ??? ????? ???? ?? ??

app:
  translate:
    timeout-ms: 3000

papago:
  base-url: https://papago.apigw.ntruss.com
  client-id: ${PAPAGO_CLIENT_ID}
  client-secret: ${PAPAGO_CLIENT_SECRET}

openai:
  secret-key: ${OPENAI_SECRET_KEY}
  chat-url: https://gms.ssafy.io/gmsapi/api.openai.com/v1/chat/completions
  chat-model: gpt-4.1-mini

circle:
  api:
    url: https://api.circle.com/v1/w3s
    key: TEST_API_KEY:ec7bb7f5221b5a64060ff39d8c1da625:e4509722324693297ca398b2a51c707d

infura:
  url: https://polygon-amoy.infura.io/v3/d4c6f7b6c50040eeacfa2f575c9a58dc

# ??? ???? ??
contract:
  address: 0x42106dd5fae320c77de6ba756b9979a7339a203a

# ?? ??? ?? ?
wallet:
  private:
    key: 0x1a097c962c0682060c91631141628d58470884589a770f93504bbcbd3197834e